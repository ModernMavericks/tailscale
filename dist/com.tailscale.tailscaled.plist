<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>Label</key>
  <string>com.tailscale.tailscaled</string>
  <!-- tailscaled has no native daemonize; the LaunchDaemon runs it foreground as root, RunAtLoad +
       KeepAlive. Explicit --state/--socket = Tailscale's native darwin defaults (Task 0: no path patch),
       so the menu-bar GUI's default -socket agrees with zero flags. A root tailscaled creates a real
       utun on 10.9 (proven on this box). -->
  <key>ProgramArguments</key>
  <array>
    <string>/usr/local/sbin/tailscaled</string>
    <string>--state=/Library/Tailscale/tailscaled.state</string>
    <string>--socket=/var/run/tailscaled.socket</string>
  </array>
  <key>RunAtLoad</key><true/>
  <key>KeepAlive</key><true/>
  <key>StandardOutPath</key><string>/Library/Logs/Tailscale/tailscaled.log</string>
  <key>StandardErrorPath</key><string>/Library/Logs/Tailscale/tailscaled.log</string>
</dict>
</plist>
